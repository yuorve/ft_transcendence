import {
  AbstractNamedAudioNode,
  _AudioAnalyzer,
  _GetVolumeAudioProperty,
  _GetVolumeAudioSubNode
} from "./chunk-PUTQWIL7.js";

// node_modules/@babylonjs/core/AudioV2/abstractAudio/abstractAudioBus.js
var AbstractAudioBus = class extends AbstractNamedAudioNode {
  constructor(name, engine) {
    super(
      name,
      engine,
      3
      /* AudioNodeType.HAS_INPUTS_AND_OUTPUTS */
    );
    this._analyzer = null;
  }
  /**
   * The analyzer features of the bus.
   */
  get analyzer() {
    return this._analyzer ?? (this._analyzer = new _AudioAnalyzer(this._subGraph));
  }
  /**
   * The output volume of the bus.
   */
  get volume() {
    return _GetVolumeAudioProperty(this._subGraph, "volume");
  }
  set volume(value) {
    const node = _GetVolumeAudioSubNode(this._subGraph);
    if (!node) {
      throw new Error("No volume subnode");
    }
    node.volume = value;
  }
  /**
   * Releases associated resources.
   */
  dispose() {
    var _a;
    super.dispose();
    (_a = this._analyzer) == null ? void 0 : _a.dispose();
    this._analyzer = null;
    this._subGraph.dispose();
  }
};

export {
  AbstractAudioBus
};
//# sourceMappingURL=chunk-FYW575UP.js.map
